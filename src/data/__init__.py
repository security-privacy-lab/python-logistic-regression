from data.breastCancer import x_train, x_test, y_train, y_test, x, y, x_scaled_train, x_scaled_test
from data.constants import PORT, SERVER_IP, ADDRESS_TUPLE, FORMAT
from data.rotten_movie_word_sentiment import x_train, x_test, y_train, y_test
from data.all_mpnet_base_v2 import x_train, x_test, y_train, y_test